<html>
<head>
 <title>Upload an Image</title>
</head>
<body>

<div class="panel-body">
	{{#each images}}
		<div class="col-md-4 text-center" style="padding-bottom: 1em;"><a href="../images/{{ id }}"><img src="../{{path}}" alt="{{title}}" style="width: 200px; height: 200px;"  class="img-thumbnail"></a></div>
	{{/each}}
</div>


</body>
</html>